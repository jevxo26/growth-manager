[variables]
NIXPACKS_INSTALL_CMD = "npm install"

[providers.node]
nodeVersion = "22"

[phases.setup]
nixPkgs = ["...", "nodejs", "chromium", "glib"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
